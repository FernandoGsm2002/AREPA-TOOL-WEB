# =====================================================
# VARIABLES DE ENTORNO PARA INTEGRACIÓN DHRU
# =====================================================
# 
# Copiar este archivo y configurar en Vercel Dashboard
# Settings → Environment Variables
#
# =====================================================

# ============= API KEYS PARA CLIENTES DHRU =============
#
# TÚ GENERAS ESTAS KEYS para cada cliente/reseller
# Cada web DHRU que se conecte tiene su propia key
#

# Key principal (tu propio DHRU - leope-gsm.com)
DHRU_API_SECRET=genera-tu-key-principal-aqui

# Keys adicionales separadas por coma (para múltiples resellers)
# DHRU_API_KEYS=key_cliente2,key_cliente3,key_cliente4

# EJEMPLO de cómo generar una key segura:
# PowerShell: [System.Guid]::NewGuid().ToString() + "-" + (Get-Random -Maximum 9999)
# Linux/Mac:  openssl rand -hex 32
# Online:     https://generate-random.org/api-key-generator

# ============= SUPABASE =============
# URL de tu proyecto Supabase
NEXT_PUBLIC_SUPABASE_URL=https://lumhpjfndlqhexnjmvtu.supabase.co

# Anon Key (pública - para cliente)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service Role Key (PRIVADA - solo en servidor, bypass RLS)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============= DHRU DE TU HERMANO (PARA ENVIAR) =============
# Estas son para cuando TÚ envías a su DHRU (ej: bypass aprobados)
DHRU_API_KEY=Q85-F15-4ZF-NFS-FBE-MRT-SFR-KTW
DHRU_API_URL=https://www.leope-gsm.com

# ============= OPCIONAL =============
# Para envío de emails (si implementas)
# SENDGRID_API_KEY=tu-sendgrid-key
# RESEND_API_KEY=tu-resend-key

# =====================================================
# RESUMEN DE ARQUITECTURA DE API KEYS
# =====================================================
#
#   TU SERVIDOR (ArepaTool API)
#   ┌─────────────────────────────────────────────────┐
#   │                                                 │
#   │  Acepta peticiones de múltiples DHRUs:          │
#   │                                                 │
#   │  ┌──────────────┬───────────────────────────┐   │
#   │  │ API KEY      │ CLIENTE                   │   │
#   │  ├──────────────┼───────────────────────────┤   │
#   │  │ ABC123...    │ leope-gsm.com (tuyo)      │   │
#   │  │ DEF456...    │ reseller1.com             │   │
#   │  │ GHI789...    │ reseller2.com             │   │
#   │  └──────────────┴───────────────────────────┘   │
#   │                                                 │
#   │  Cada reseller configura SU DHRU con la key    │
#   │  que TÚ le proporcionas                         │
#   │                                                 │
#   └─────────────────────────────────────────────────┘
#
# =====================================================
# NOTAS IMPORTANTES:
# - NO subir este archivo a Git con valores reales
# - Configurar en Vercel Dashboard, no en código
# - Cada reseller tiene su propia API KEY única
# - Puedes deshabilitar un reseller quitando su key
# =====================================================
